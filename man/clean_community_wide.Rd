% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_data.R
\name{clean_community_wide}
\alias{clean_community_wide}
\title{Clean community data in wide format}
\usage{
clean_community_wide(
  x,
  community_col = "comm",
  time_col = "time",
  na_zero = FALSE,
  filter_transient = FALSE,
  empty_years = FALSE,
  min_samples = 0.7
)
}
\arguments{
\item{x}{A data.frame. Community matrix with time in rows and taxa in columns.}

\item{community_col}{Character. Name of column with the community identifier. Default "comm".}

\item{time_col}{Character. Name of column with time variable. Default "time".}

\item{na_zero}{Boolean. Replace missing values (NAs) with zeros (0). Default FALSE}

\item{filter_transient}{Boolean. Filter transient species Default FALSE}

\item{empty_years}{Boolean. Remove empty years. Default FALSE.}

\item{min_samples}{Numeric. Minimum proportion (between 0 and 1) of valid data points to keep a species in the data. Default 0.7.}
}
\value{
A data.frame with the community data in wide format.
}
\description{
Clean community data in wide format
}
\examples{
require(detrending)

data(example_data_wide) # Load sample data
clean_community_wide(x = example_data_wide,
                     community_col = "comm",
                     time_col = "time")
}
\author{
Héctor Miranda-Cebrián, \email{hectorm94@gmail.com}
}
