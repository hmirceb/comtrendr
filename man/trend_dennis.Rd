% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trends.R
\name{trend_dennis}
\alias{trend_dennis}
\title{Estimate mean annual growth rate following Dennis et al. (2001).}
\usage{
trend_dennis(x, time = NULL)
}
\arguments{
\item{x}{Numeric. A vector of abundances.}

\item{time}{Numeric. A vector with the time steps corresponding to each value in x. If not provided data are assumed to be in order.}
}
\value{
A named vector with the mean annual growth rate of a population in the natural logarithm scale, its confidence interval and p-value.
}
\description{
This function estimates the mean annual growth rate of a time series of abundance data following Dennis et al. (2001) (see Details).
}
\details{
For a given a time series of abundances \eqn{n_{t}} the function estimates its mean growth rate \eqn{\mu} and variance \eqn{\sigma^2} using a linear regression model without intercept such as:
\deqn{y_{i} \sim \mu t_{i} + \epsilon_{i} }
\deqn{\epsilon_{i} \sim Normal(0, \sigma^2)}
where \eqn{ y_{i} = \dfrac{ \ln{ ( n_{t}/n_{t-1}) } }{ t_{i} } } and \eqn{ t_{i} = \sqrt{ t_{t}-t_{t-1} } }

Note that the confidence intervals for the estimate of \eqn{\mu} are not reliable.
}
\examples{
require(comtrendr)

# Simulate time series
ts <- 5^seq(1, 2, by = 0.01)
mean(diff(log(ts))) # True trend = 0.016 (~1.62\%)
# Simulate some random noise
noise <- rnorm(length(ts))

# Estimate trend
trend_dennis(ts+noise)
}
\references{
\itemize{
\item Dennis, B., Munholland, P. L., & Scott, J. M. (1991). Estimation of Growth and Extinction Parameters for Endangered Species. Ecological Monographs, 61(2), 115–143.
}
}
\author{
Héctor Miranda-Cebrián, \email{hectorm94@gmail.com}
}
