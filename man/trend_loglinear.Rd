% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trends.R
\name{trend_loglinear}
\alias{trend_loglinear}
\title{Estimate mean annual growth rate using linear regression}
\usage{
trend_loglinear(x, time = NULL)
}
\arguments{
\item{x}{Numeric. A vector of abundances.}

\item{time}{Numeric. A vector with the time steps corresponding to each value in x. If not provided data are assumed to be in order.}
}
\value{
A named vector with the slope of a linear regression of the log transformed abundances, its confidence interval and p-value.
}
\description{
Estimate mean annual growth rate using linear regression
}
\details{
For a given a time series of abundances \eqn{n_{t}} the function estimates a linear regression model with log-transformed abundances as the response variable and time steps (with \eqn{t_{0} = 0, t_{1} = 1...}) as the explanatory variable.
}
\examples{
require(comtrendr)

# Simulate time series
ts <- 5^seq(1, 2, by = 0.01)
mean(diff(log(ts))) # True trend = 0.016 (~1.62\%)
# Simulate some random noise
noise <- rnorm(length(ts))

# Estimate trend
trend_loglinear(ts+noise)
}
\author{
Héctor Miranda-Cebrián, \email{hectorm94@gmail.com}
}
